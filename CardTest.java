import org.junit.Test;
import static org.junit.Assert.assertEquals;

class CardTest {
    @Test
    public void checkCardColour(){
        Card testCard = new Card ("Green" , 2,4);
        assertEquals("Green" , testCard.getcolour());

    }
    public void checkCardColour(){
        Card testCard = new Card ("Green" , 2,4);
        assertEquals( , testCard.getCoordinates());

    }

}